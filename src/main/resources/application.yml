server:
  port: 8080

spring:
  profiles:
    default: ollama

---
spring:
  config:
    activate:
      on-profile: openai
  ai:
    model:
      chat: openai
    openai:
      api-key: 1234
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

---
spring:
  config:
    activate:
      on-profile: ollama
  ai:
    model:
      chat: ollama
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.1
          temperature: 0.7
